# Hugging Face Spaces README frontmatter (keep out of GitHub README.md —
# GitHub renders YAML --- blocks as a metadata table).
# When deploying to a Space, prepend this as:
#   ---\n<title fields>\n---\n
# above the README body, or set Docker + port 7860 in the Space UI.

title: Profile RAG Agent
emoji: 💬
colorFrom: green
colorTo: gray
sdk: docker
app_port: 7860
pinned: false
license: mit
